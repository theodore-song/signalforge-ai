# Optional: live US stock snapshots (https://alpaca.markets/data)
ALPACA_API_KEY=
ALPACA_API_SECRET=

# Optional: server-side AI investment committee brief
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini

# Optional JSON feed adapters. Each URL should return the schema documented in README.md.
POLITICIAN_TRADES_URL=
INSTITUTIONAL_HOLDINGS_URL=
CROWD_SENTIMENT_URL=

# Required for durable user accounts, multiple portfolios, sessions, and scheduled AI agents.
# The legacy KV names and current Upstash names are both supported.
KV_REST_API_URL=
KV_REST_API_TOKEN=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Protect /api/cron in production
CRON_SECRET=
